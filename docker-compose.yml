version: '3.8'

services:
  lms-suite:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - ./backend/.env
    environment:
      - PORT=8000
      - ALLOWED_ORIGINS=http://localhost:8000,http://127.0.0.1:8000
    restart: always
